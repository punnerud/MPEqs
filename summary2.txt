================================================================================
PHASE 92 — MPEqs' generic solver library: the record grows by CLASSES
================================================================================

Phase 91 gave the record a formula library — one relation per entry. This gives it a
SOLVER library: parameterised machines for whole problem classes, each taking a typed
spec (never code), executing exactly in Fractions or integers, refusing by name, and
reporting its work so a runaway search dies instead of hanging.

    15 solvers: search, linear_system, quadratic, crt, gcd_lcm, series,
    combinatorics, recurrence, rate_work, ratio_split, mixture, interest,
    base_convert, digit_ops, factor
    22 search predicates, 45 wordings (the lexical face, for embedding proposal)
    22/22 self-test cases exact, 6/6 refusals named and correct

The load-bearing one is SEARCH, because so many olympiad questions are "the <aggregate>
of all n in <domain> such that <conditions>": a domain (integer range, divisors of N,
divisors of k! via Legendre so 13! is never built), a conjunction of predicates, an
aggregate, and a post-op — which is exactly how AIME's "answer mod 1000" is said.

AND IT SOLVES A REAL AIME PROBLEM BY CHAINING, WITH NO MODEL AT ALL. "The sum of all m
with 13!/m a perfect square is 2^a 3^b 5^c 7^d 11^e 13^f; find a+b+c+d+e+f": search
returns 9,698,458,770, factor returns 2·3²·5·7³·11·13⁴, the exponent sum is 12 — the
published answer, and the problem phase 89's solo arms got right only 2 times in 10.

FOUR SELF-TEST FAILURES, AUTOPSIED: three were MY hand anchors (the 13! sum invented as
4989600 when hand derivation gives 9698458770; eight digit-sum multiples of 7 where
there are ten; 43/100 for a mixture that is 41/100), one was a REAL code bug (factor
with k=13 still demanded an "n", and the dispatcher reported the KeyError honestly as
an error rather than an answer). The tally across the study is now 7 hand-anchor errors
against 1 machinery error, and the machinery error was caught by the anchors — which is
the whole argument for writing truths before code.

================================================================================
PHASE 93 — mapping problems onto the solver library, and a hypothesis that died
================================================================================

Thirty problems (two per solver, distractor quantities throughout, truths verified
against the library BEFORE any model call — which caught three more of my anchors:
273 not 812, a(5)=111 not 63, digit sum 21 not 24). The model emits a JSON spec; the
record validates and executes; nobody computes but the record.

    embedding alone picks the right solver        28/30
    FREE   (whole catalogue, model chooses)       solver right 27/30, all 30 specs
                                                  parsed and ran -> EXACT 29/30
    GUIDED (embedding chooses, model fills only)  solver right 26/30, 28 parsed
                                                  -> EXACT 26/30

TWO FINDINGS, BOTH AGAINST WHAT I WROTE IN THE FILE'S OWN DOCSTRING BEFORE RUNNING IT:

  GUIDING MADE IT WORSE. The division of labour that has won at every earlier scale —
  narrow the model's job to slot-filling — LOSES here, 26 against 29. The two embedding
  misses propagate into confident wrong answers (a search problem forced through
  digit_ops returns 1; an arithmetic series forced through recurrence returns 487),
  and two mixture specs simply never came. Seeing the whole catalogue is itself
  information: at 35B, choosing was not the bottleneck, and taking the choice away
  removed a correction the model was making for free.

  A WRONG CHOICE DOES NOT SELF-DESTRUCT. Both arms refused ZERO specs. The slots are
  generic enough that a mis-chosen solver still fills and still runs, so the record's
  validation catches malformed specs but not misapplied ones. The delivery gate this
  study wants at spec level does not exist yet — that is now a measured hole, not an
  assumption.

And the one FREE "error" is not one: the CRT problem was mapped onto search over a
range with two mod_eq conditions and aggregate "list", which found exactly [13] — the
right number in the wrong shape. Alternative valid mappings are real (the label is not
the only correct answer), and a shape-normalising comparator would score FREE 30/30.
Reported both ways rather than rescored.

================================================================================
PHASE 95 (built before phase 94's evidence, deliberately) — the library grows by
expressions: tuples, polynomials, coordinates, modular arithmetic
================================================================================

Phase 92's search has a fixed predicate list and ONE variable, which is exactly where
competition problems escape it: they ask about pairs and triples related by arithmetic
nobody enumerated in advance. The general move is to let conditions be EXPRESSIONS,
written as ordinary strings — because format is capability (phase 84), and
"a*a + b*b == c*c" is something any model can write while nested predicate JSON is not.

Safety is phase 90's discipline reused: the string goes through Python's own parser and
is walked against a whitelist of node types and function names, so what executes is
arithmetic over bound variables and nothing else.

    multisearch  1-4 variables with ranges, optional strict ordering, expression
                 conditions tested AS SOON AS their variables bind (so triples are
                 feasible), an objective expression, aggregates, mod post-op
    polynomial   exact rational roots (verified, not guessed), Vieta, evaluation
    geometry     exact coordinates: squared distance, shoelace area, collinearity,
                 circle through three points, line intersection
    modular      modpow, inverse, multiplicative order, totient

    15/15 self-test cases exact, 6/6 refusals named — three of them attempted
    escapes from the expression sandbox (an __import__ call, an attribute access,
    a variable-exponent power), all refused by the AST walk before evaluation
    MPEqs now holds 19 solvers; expressions admit 14 functions over up to 4 variables

Written and self-tested BEFORE the coverage sweep of phase 94 ran, so that phase's
named-missing list is an honest test of whether this growth was aimed at the right
thing — a prediction, registered by timestamp in the repository, not a rationalisation.

================================================================================
PHASE 94 — how much of AIME the vocabulary actually reaches: measured, and it is 0
================================================================================

Thirty real AIME problems, the whole 15-solver catalogue in the prompt, and one
instruction: map it onto a solver, or NAME what is missing. The record then runs every
spec it is handed and compares to the published answer.

FIRST SWEEP WAS A HARNESS FAILURE, AND THE AUTOPSY SAID SO. Twenty-two of thirty replies
read as "unparseable" — which would have looked like a coverage result. The raw replies
were 2,400-3,500 characters of PROSE WORKING: the model had ignored the output format and
started solving instead of mapping. Prefilling the JSON opening brace into the assistant
turn removed the prose branch entirely — one character, phase 84's law at 35B — and the
sweep was re-run.

THE REAL NUMBERS, and they are stark:

    claimed a mapping      22/30   (declined 5, still unparseable 3)
    the spec ran           19      (the record refused 3 as malformed)
    exact vs published     0       (wrong 19)

    solvers reached for: search 16, linear_system 3, quadratic 2, recurrence 1
    named as missing: geometric probability, symbolic algebra and logarithms,
    nonlinear systems with radicals, geometric reasoning (x2)

Three things follow, none of them comfortable:

  WILLINGNESS TO MAP IS NOT REACH. Told to map, the model maps anything — a tennis
  tournament probability became a sum over a range with condition "eq 1"; a cube-geometry
  volume became an identity linear system. Nineteen confident wrong numbers came out of
  a pipeline whose every component is exact.

  THE BOTTLENECK IS THE MAPPING, NOT THE MACHINES. Problem 0 IS in vocabulary — phase 92
  solved it by hand-chaining search and factor to the published answer 12 — and the model
  chose the right domain (divisors_of_factorial, k=13) but passed arg=13 instead of 13!.
  A parameter, not a capability.

  A DELIVERY GATE AT SPEC LEVEL IS NOT OPTIONAL. Phase 93 measured that mis-mappings
  never self-destruct; this measures what that costs on real problems: everything.

================================================================================
PHASE 96-97 — the spec-level gate, and an ablation that prices every layer
================================================================================

Phase 93 measured the hole (mis-mappings never self-destruct); phase 94 measured what
it costs (19 confident wrong AIME answers). The gate is built from two mechanisms this
study proved separately: VALUE ECHO (every literal in the spec must appear in the
problem or be a structural constant — phase 91) and TWO MACHINES (the model gives a
second spec that must use a DIFFERENT solver, and the record runs both — phase 74;
unlike phase 89's two phrasings, these are two computations, not two readings).

Run on two arms: the 30-problem battery where mappings are mostly right, and 15 AIME
problems where phase 94 measured every mapping wrong. Replies are cached and the specs
recorded, so the ablation below costs no GPU and runs over IDENTICAL data.

    BATTERY (n=30)                          AIME (n=15, all mappings wrong)
    gate    delivered right WRONG           delivered right WRONG
    none          30    29     1                   8     0     8
    echo          13    13     0                   1     0     1
    agree          5     5     0                   2     0     2
    both           3     3     0                   0     0     0
    both+          4     4     0                   1     0     1

Four findings, all uncomfortable in the useful way:

  ONLY THE FULL COMPOSITION NEVER LIES. "both" is the single row with zero wrong
  deliveries on the hard set — and it gives back 10% of the easy set. On this evidence
  lie-freedom costs ninety percent of coverage. That is the exchange rate, measured.

  AGREEMENT ALONE IS WEAK HERE. Two different machines agreed on garbage twice on AIME:
  when both roads collapse to a trivial value (a sum over an empty condition, a count
  of nothing) they agree perfectly. Structural difference is not independence.

  ECHO ALONE IS THE BEST SINGLE LAYER. Recall 0.43 at one lie — better than agreement
  on both axes, and it costs no model call at all. Cheap mechanical checks keep beating
  expensive social ones, which is this study's oldest result.

  RELAXING ECHO BUYS ALMOST NOTHING. Allowing digit-scale constants (0-12, and the
  loop bounds a digit search needs) as structure recovers one battery problem and lets
  one AIME lie through. The strict rule is the better trade.

Also measured, no model calls: 29 of 30 PLANTED mis-mappings caught (right numbers,
wrong machine), 12 of them by echo alone.

================================================================================
PHASE 98 — MPEqs on GSM8K: the arith solver, shape over name, and an honest loss
================================================================================

Two additions first, both forced by evidence rather than taste:

  ARITH. Word problems are a handful of quantities combined in a few steps, and the
  library had no machine for that — phase 45's plan graph, reborn as a library member:
  named steps, each an expression over the problem's numbers and earlier steps, all
  evaluated exactly in Fractions inside the same AST sandbox that guards multisearch.
  Library now 20 solvers; 18/18 self-tests exact, 8/8 refusals named.

  SHAPE DISPOSES OVER NAME. The 1B immediately wrote a perfect arith body under the
  name "search". Phase 91's law one level up: when the NAMED solver fails, the SLOTS
  are allowed to say which machine this is, the repair is attempted, and every repair
  is COUNTED rather than performed silently. Fired 3 times in the run below, all
  ('search'|'linear_system') -> 'arith'.

THIRTY GSM8K PROBLEMS, THREE ARMS:

    SOLO-35B    28/30 — the model simply answers
    MPEqs-35B   spec parsed 29, ran 26 -> 21 exact, 5 wrong
    MPEqs-1B    spec parsed 16, ran 3 -> 1 exact

ROUTING THROUGH EXACT MACHINERY LOSES HERE, 21 AGAINST 28, AND THAT IS THE RESULT.
Grade-school arithmetic is something a 35B already does well; forcing it through a spec
adds mapping errors without removing any error that was there. The machinery pays where
the model is weak — exact fractions, unit routing, long searches, reversibility,
memory — and this is the measurement that says where that is NOT.

THE 1B RESULT IS THE SAME LAW AS PHASE 84. It produces JSON-shaped text (16 of 30
parsed) that is almost never a valid spec (3 ran). Making the task mechanical does not
help a model that cannot follow the mechanism; the floor is instruction-following, not
arithmetic.

WHAT DOES HELP IS CROSS-KIND AGREEMENT. Solo answering and spec-and-execute are two
DIFFERENT KINDS of work, not two phrasings, and gating on their agreement behaves
accordingly:

    echo alone            delivered 16, right 15, WRONG 1   precision 0.94
    agrees with solo      delivered 22, right 21, WRONG 1   precision 0.95
    both                  delivered 15, right 15, WRONG 0   precision 1.00

Against phase 89, where two PHRASINGS of the same kind of work agreed on the same
wrong number twice in three deliveries. The rule this study keeps re-deriving: what
makes agreement worth anything is that the two roads are different KINDS, and the
cheapest mechanical check is worth more than another opinion.

================================================================================
PHASE 99 — the vocabulary's ceiling on AIME: mapping failure, not reach failure
================================================================================

Phase 94's zero was compatible with two opposite diagnoses — the library cannot express
these problems, or it can and the mapper missed — and those call for opposite work. So
the same fifteen problems were mapped BY HAND, no model in the loop, and executed:

    hand-written specs, exact against the published answers   5/15
    the model's mapping on the same problems (phase 94)       0/19 that ran

    [ 0] sum of m with 13!/m square, then add the exponents          12  EXACT
    [ 1] sum of C(C(n,2),2) for n = 3..40, mod 1000                   4  EXACT
    [ 7] 2-colourings of a 12-gon with no monochromatic rectangle   928  EXACT
    [12] adults 5/12 of a crowd, 11/25 after 50 arrive, minimised   154  EXACT
    [13] least multiple of 23 that is 1 mod 2^n, counting fixed n   363  EXACT

A THIRD OF THESE AIME PROBLEMS IS EXPRESSIBLE IN TWENTY SOLVERS AND AN ARITHMETIC
SANDBOX, AND THE MAPPER FOUND NONE OF THEM. The bottleneck is named: not the machines,
not the vocabulary — the mapping. Problem 7 is the sharpest case: a rectangle in a
regular 12-gon is two diameters whose four vertices share a colour, so the whole thing
is fifteen mechanical conditions over the 4096 bit-patterns of one variable, and the
answer falls out exactly. Nothing about that is beyond a 35B to write; it simply did
not.

Two additions were forced by problems in this set, declared as such rather than
motivated by taste: pow2 and inv_mod inside the expression sandbox (problem 13 needs a
modular inverse at a variable power of two, which the literal-exponent guard blocks on
purpose), and an exponent_sum post-op (AIME asks for the exponents of a prime
factorisation to be added constantly; without it problem 0 needs two chained specs).
Library: 20 solvers, 16 expression functions, 18/18 self-tests, 8/8 refusals.

THE TEN OUT OF REACH NAME THE SAME THREE THINGS EVERY TIME: plane and solid geometry
(five of them), quantifiers over structures rather than integers — families of subsets,
uniqueness of roots (three), and expectation under an adaptive strategy (two). That is a
build list, not a mystery.

================================================================================
PHASE 100 — teaching the mapping, and what the refusals demanded next
================================================================================

Phase 99 left one lever: the model had been told the VOCABULARY but never shown the
SHAPE of the work. So three worked mappings went into the prompt — a bit-pattern
enumeration, a two-variable Diophantine minimisation, an aggregated expression with a
mod post-op — all invented for the purpose so nothing leaks from the test set. Same
single call per problem; no retry, no feedback, no second look.

    claimed 10/15, ran 3, EXACT 1 (wrong 2, refused 7, declined 3, unparseable 2)
    of the 5 problems proved reachable: 1 solved
    baselines: no examples 0 exact; hand-mapped ceiling 5

One of five, from zero. Worked examples move the mapping, and they do not close it.

THE SEVEN REFUSALS WERE THE REAL YIELD — a build list written by the failures:

  A BUDGET DEATH THAT WAS THE MACHINE'S FAULT, NOT THE MODEL'S. The model wrote the
  adults-at-a-concert problem as two variables to 10,000 with two linear equations —
  10^8 tuples, refused — where my hand spec used tighter bounds. The fix is generic and
  belongs in the machine: when the innermost variable appears in a LINEAR equality,
  SOLVE for it instead of scanning it. The model's own rejected spec now runs in 10,001
  steps instead of 10^8.

  AND THE FIRST VERSION OF THAT FIX WAS UNSOUND, CAUGHT BY THE SELF-TESTS. I implemented
  it as phase 75's probe trick — sample the residual at 0, 1, 2, and if the three points
  are collinear, solve. digit_sum(n) - 10 sampled at 0, 1, 2 gives -10, -9, -8: a perfect
  straight line, so the solver skipped the scan and returned 0 where the truth was 10.
  THREE SAMPLES CANNOT CERTIFY LINEARITY; THE SYNTAX CAN. Replaced with a structural AST
  check (a call containing the variable is never linear, a power never is, a product only
  when one side is free of it), and the speedups survive exactly where they are sound:
  a+b=20 walks 27 states instead of 361, digit_sum falls back to the full 1,010, the
  hand-written AIME spec walks 2,004 instead of 102,051.

  SMALLER ONES, ALSO GENERIC: an "eval"/"only" aggregate for degenerate one-hit searches
  (the model asked for it by name), and a decline written as {"solver":"none"} which the
  harness had been scoring as a refusal.

RE-EXECUTING THE SAME SPECS AGAINST THE GROWN MACHINE, no new model calls: refusals fell
7 -> 5 and two more specs ran — and BOTH are wrong (25 against 244, and 550 against 154,
the latter because the model assumed all fifty new arrivals were adults). Growing the
machine converts refusals into answers, not into correct answers. The mapping is still
where the error lives, and every layer that could hide that has now been measured
separately.

================================================================================
PHASE 101 — retrieved exemplars: retrieval is perfect, the mapper still is not
================================================================================

Phase 100's three fixed examples could not cover the shapes the reachable problems
need, so the bank grew and became retrievable: twelve exemplar mappings (invented, no
leakage), each embedded with numbers masked, the two nearest shown per problem. Phase
88's translation memory moved up a level — the library's memory of HOW ITS MACHINES GET
USED.

    retrieved the needed shape for the reachable five   5/5
    claimed 10, ran 6, EXACT 1 (of the reachable five: 1)
    baselines: no examples 0, fixed three 1, hand-mapped ceiling 5

RETRIEVAL IS NOT THE PROBLEM. Every one of the five reachable problems had the right
exemplar in front of the model — factorial_divisors for the 13! question, bits for the
dodecagon, diophantine for the concert crowd, modular_inverse for the powers of two —
and one came back right. The decomposition is now complete, each layer measured
separately:

    machines        exact, 20 solvers, 18/18 self-tests, refusals named
    vocabulary      reaches 5 of 15 (proved by hand)
    retrieval       puts the right shape up 5 of 5
    THE MAPPER      converts 1 of 5

Whatever is missing lives in the step where a problem in language becomes a filled
spec, with the machine, the vocabulary and the example all present. That is a clean
result and an unglamorous one: nothing else in the pipeline is worth optimising until
it moves.

One harness bug caught on the way, and it would have poisoned everything downstream:
the encoder returned 17 vectors for 15 problems, because a LaTeX-dense statement
tokenises past the 512-token window and llama-embedding silently emits an extra row —
which shifts every later vector onto the wrong problem. Stripping the markup before
embedding fixed it and improved the key, since the shape lives in the words, not the
typesetting.

================================================================================
PHASE 102 — where MPEqs wins: the band between easy sums and real mathematics
================================================================================

Phase 98 measured an honest loss (GSM8K: solo 28/30, machinery 21/30) and phase 94 a
collapse (AIME: mapping 0). Between them is the band this apparatus exists for: problems
whose MAPPING is one sentence and whose ARITHMETIC is out of reach. Twenty of them,
truths computed before any model call, in four families — twelve-step exact fraction
folds, big-integer products and powers, counts over hundreds of thousands, divisor
structure of large factorials.

    SOLO-35B    0/20      not one, and never for want of trying: it answers every time
    MPEqs       8/20      first run, specs parsed 19, ran 14

THEN THE FAILURES NAMED FIVE MISSING MACHINES, and every one was a real gap:

  EXACT RATIONALS — a correctness BUG, not a preference: "5/11 * 5/11 * 5/11" evaluated
  in floats and the record returned 2774945224945457/225179981, the exact binary
  expansion of a rounded double, where the truth is 125/1331. Integer literals are now
  wrapped as Fractions whenever an expression divides, so `/` is exact; integer searches
  keep integer speed (a 200,000-wide digit search still runs in 0.2 s).

  ^ MEANS POWER. The model wrote (-1)^1 and Python read a bitwise xor. In a mathematics
  spec language the caret is a power, and xor was never in the whitelist anyway.

  A FOLD MACHINE. "Nine times in a row, add 2/5 and then multiply by 3/4" is neither an
  arith chain (no loop) nor a multisearch (a map-reduce is not a fold). The new iterate
  solver carries an accumulator across k, exactly.

  VARIABLE EXPONENTS ON A BOUNDED BASE. (-1)^k is the alternating sign half these
  problems need; the literal-exponent guard exists to stop unbounded growth, and a base
  of absolute value at most one cannot grow.

  PRODUCT AS AN AGGREGATE, and six variables instead of four.

    MPEqs after the growth   12/20     (specs parsed 19, ran 18)
    by family: fractions 4/5 (was 0), big 3/5, count 2/5, divisors 3/5
    SOLO-35B, unchanged      0/20

TWO MORE ROUNDS OF THE SAME LOOP, each driven by an autopsy rather than a hunch:

  A REPORT VOCABULARY. Several machines return a record — gcd AND lcm, divisor count
  AND divisor sum — and nothing in a spec said which number was being asked for, so a
  right answer inside a dict scored wrong. A spec may now carry "report", the dispatcher
  routes by it (asking digit_ops for a divisor sum now lands in factor), and the literal
  exponent cap went from 64 to 1024 because 2^200 is bounded, not unbounded.

  AND THAT ROUND REGRESSED THE DIVISOR FAMILY, 3 to 2, FOR A REASON WORTH KEEPING. When
  "report" was appended to the factor schema line, the k-versus-n distinction — k means
  k FACTORIAL — slid to the end of the sentence, and the model began writing {"n": 25}
  for "25 factorial". A schema is read literally; a buried distinction is a lost one.
  Rewriting that line to lead with the factorial case took the family from 2 to 5.

    MPEqs, final     15/20   (fractions 4, big 4, count 2, divisors 5; wrong 2)
    SOLO-35B         0/20    still not one, still never abstaining

    the progression: 8 -> 12 -> 12 -> 15, and every step was a failure autopsied
    into either a missing machine or a misread schema

Twelve against zero. The same machinery that LOSES to the model on grade-school sums
beats it twelve to nothing where the arithmetic is real — and the library grew by what
the problems asked for, four extra solves bought by five fixes that the failures wrote
themselves. Library: 21 solvers, 23/23 self-tests exact, 9/9 refusals named.

================================================================================
PHASE 103 — the gate does not transfer: different band, different failure mode
================================================================================

Phase 102's win came with an uncomfortable detail: MPEqs answers 12 of 20 correctly and
6 wrongly, and nothing in that arm says which is which. So phase 97's gate was carried
into the winning band — two machines plus value echo — and it does not survive the
journey.

    gate      delivered  right  WRONG        (hard-arithmetic battery, n = 20)
    none            16     12      4
    echo             7      4      3
    echo relaxed     9      6      3
    agree            0      0      0
    both             0      0      0

TWO MECHANISMS, BOTH DEAD, FOR REASONS THAT ARE PROPERTIES OF THE BAND:

  AGREEMENT IS UNAVAILABLE. A second, genuinely different machine was offered for 17 of
  20 problems and ran for 8, and it never agreed. There is no second way to compute
  987654321 x 123456789 in this library — one natural machine per problem is not a
  weakness of the mapper, it is what an exact arithmetic question IS. Phase 97 measured
  agreement as the weaker layer; here it is not a layer at all.

  VALUE ECHO IS ACTIVELY HARMFUL. Precision falls from 0.75 ungated to 0.57 with echo,
  because in this band the literals are DERIVED rather than invented: the twelve-step
  fold needs the 3 and 4 inside (k+2), the six-digit palindrome count needs 100000 and
  999999 that the problem says only as the word "six-digit". Echo was built to catch a
  hallucinated bound; here it catches arithmetic.

WHAT REMAINS TRUE IS THE COMPARISON THAT MATTERS. The model alone on this battery:
0 right, 20 confident wrong, 0 abstentions. MPEqs ungated: 12 right, 4 wrong, 4 flagged
by the machines themselves (refusals and unparsed specs). Routing through the machinery
takes the lie rate from 100% to 20% and correctness from 0% to 60% — without any gate
at all, because the machines' own refusals are already a partial gate.

The general law, now with two sides measured: a delivery gate is only as good as the
independence it can find. Where the risk is MAPPING, two entry points and a value echo
find it cheaply. Where the risk is ARITHMETIC, both mechanisms have nothing to grip,
and the honest report is the residual error rate — not a gate that flags everything and
calls it safety.

One more bug caught, in the gate itself: isinstance(True, int) is true in Python, so a
JSON boolean inside a spec was being reported as an invented literal named "True".
Phase 97's numbers were checked against the fix and are unchanged (no booleans in those
specs); the hard battery had ten.

================================================================================
PHASE 104 — held out: the tuning generalises, 17/20 on problems it never saw
================================================================================

Three rounds of sharpening (missing machines, a report vocabulary, a schema line
rewritten) took the hard-arithmetic battery from 8 to 15 of 20 — against a battery I
had been reading the failures of the whole time, which is exactly how a score stops
meaning anything. So the four families were regenerated with new numbers, new ranges
and new phrasings, and run ONCE with no further tuning of anything.

    HELD OUT (n = 20)      MPEqs 17/20      specs parsed 19, ran 19, wrong 2
                           SOLO-35B 0/20    no abstentions, twenty wrong numbers
    by family              fractions 4, big 4, count 4, divisors 5

    tuned battery for comparison: 15/20

Seventeen on problems the prompt has never seen, against fifteen on the one it was
tuned against — the growth was in the machines and in schema lines that say what they
mean, not in accidental fit to twenty particular questions. The count family, weakest
at 2 on the tuned set, came in at 4 here, which is the ordinary variance of twenty
items and worth no story at all.

The band is now measured from both sides with a held-out check in the middle:

    grade-school word problems   the model alone wins       28/30 against 21/30
    hard exact arithmetic        the machinery wins         17/20 against 0/20
    competition mathematics      neither reaches            hand-mapped 5/15,
                                 the mapper 1/15

A tool earns its keep by its domain. This one's domain is now drawn with numbers on
three sides, and the honest summary of MPEqs is a sentence: it turns problems whose
arithmetic a model cannot hold into problems whose only remaining difficulty is saying
what to compute.

================================================================================
PHASE 105 — widening the band: exact probability and unit conversion
================================================================================

With the domain drawn and the mapper measured as the bottleneck for competition
mathematics, the cheap direction is to widen the band where the pipeline already works.
Two classes went in, both failing a model for the same reason the hard-arithmetic
battery does — the answer is an exact ratio it cannot hold — while the mapping stays one
sentence long:

  PROBABILITY  a uniform space of tuples, an event as more constraints, and the answer
               as a Fraction. Both counts run through the multisearch machine, so
               pruning, budgets and the expression sandbox are inherited, not rebuilt.
               The m+n report the competitions ask for is one field.
  CONVERT      the phase 73 brick router as a library member at last. Sixteen written
               conversion facts, lifted through compound type space on demand, exact,
               and refusing honestly when no path exists (kroner to kilograms).

    twenty problems, ten of each, truths computed before any model call

    SOLO-35B    1/20     probability 0/10, convert 1/10
    MPEqs      17/20     probability 9/10, convert 8/10 (parsed 20, ran 18, wrong 1)

Nine of ten exact probabilities where the model gets none: it answers 0.11 or 4/36
unreduced or simply the wrong count, and the exact m/n is what was asked. Eight of ten
conversions where it gets one: 3 miles per second is 10863072/625 km/h and no amount of
confidence produces that from mental arithmetic.

The routing machinery had existed as its own experiment since phase 73 and could not be
REACHED by a mapped problem until it became a solver with a schema line. That is the
whole lesson of this phase: a capability nobody can address is not a capability.

================================================================================
PHASE 106 — three more classes: statistics, calendar arithmetic, percentage chains
================================================================================

The band is no longer a discovery, it is a construction, and each class is another
slice. Three more, chosen by the rule phase 105 established — mechanical where the model
is weak, one sentence where the mapping is strong:

  STATISTICS  exact mean, median, both variances, exact standard deviation when it is
              rational and named irrational when it is not — never quietly rounded
  DATETIME    days between dates, weekdays, dates shifted by a thousand days, leap-year
              counts across a century boundary
  FINANCE     successive percentage changes (which do not add), annuity payments, tax

    eighteen problems, truths computed before any model call

    SOLO-35B    8/18     statistics 4/6, datetime 1/6, finance 3/6
    MPEqs      18/18     statistics 6/6, datetime 6/6, finance 6/6

Calendar arithmetic is the sharpest split: 1 against 6. "How many days from 1 January
1970 to 10 August 2026" has one right answer, 20,675, and no amount of fluency produces
it.

AND THE SAME DEFECT APPEARED A THIRD TIME. The one MPEqs miss before the fix was a tax
problem where the model wrote "rate": 25 meaning 25 percent while the slot meant a
fraction of one. Phase 102 lost a family to a buried k-versus-n distinction; phase 105
needed the unit vocabulary spelled out; here an ambiguous unit inside a slot cost the
answer. THE MAPPER'S ERRORS ARE OVERWHELMINGLY SCHEMA-READING ERRORS, not reasoning
errors — the slot now says "percent" and takes whole percents, the solver treats any
rate above one as a percentage, and the family went 5/6 to 6/6.

That is a design rule with three measurements behind it: a slot must name its unit and
lead with its distinction, because the model reads the schema literally and has nothing
else to go on.

================================================================================
PHASE 107 — does the catalogue scale? Twenty-seven problems, nine classes
================================================================================

Every class so far was measured in its own run, with its own worked example and a
catalogue that happened to hold the machine it needed. The deployment question is
harder: with twenty-six solvers in the prompt and three GENERIC examples matching no
class, can the model still pick the right machine?

    twenty-seven problems, three from each of nine classes, truths pre-computed

    SOLO-35B   7/27
    MPEqs     20/27   (parsed 25, ran 23, wrong 3)

    class          n  solo  mixed   focused
    big            3     0      2       4/5
    convert        3     0      2      8/10
    count          3     0      3       4/5
    datetime       3     1      3       6/6
    divisors       3     0      3       5/5
    finance        3     2      3       6/6
    fractions      3     0      0       4/5
    probability    3     1      1      9/10
    statistics     3     3      3       6/6

SIX CLASSES HOLD AT FULL RATE; TWO COLLAPSE — and they are exactly the two whose worked
example vanished. Fractions went 4/5 to 0/3 without the fold example and probability
9/10 to 1/3 without the probability one: the model stopped reaching for iterate and
probability at all, though both were in the catalogue with their slots spelled out.

A CATALOGUE LINE IS NOT ENOUGH FOR A MACHINE THE MODEL HAS NEVER SEEN USED. The
schema-literacy rule of phase 106 has a companion: a schema says what the slots are, an
example says that the machine EXISTS as a thing to reach for. The confound is declared —
a drop here mixes catalogue size with the loss of class-matched examples — which is
exactly why the next phase separates them.

================================================================================
PHASE 108 — retrieval closes the scaling gap: 25 of 27, and nothing wrong
================================================================================

The confound from phase 107 is removed the way phase 101 pointed to: one invented
exemplar per class in a bank, embedded with numbers masked, the two nearest retrieved
per problem, and everything else identical — same twenty-seven problems, same
twenty-six-solver catalogue, same generic instruction, same model.

    MIXED, generic examples      20/27   (3 wrong)
    MIXED, retrieved exemplars   25/27   (parsed 26, ran 25, WRONG 0)
    SOLO-35B                      7/27

    the two collapsed classes recover: fractions 0/3 -> 2/3, probability 1/3 -> 3/3,
    convert 2/3 -> 3/3, and no class loses anything

Ninety-three percent, no wrong deliveries, on nine different kinds of problem the model
alone answers correctly seven times in twenty-seven. And the recipe is now stated as
three parts, each of which was measured separately before being combined:

    A MACHINE that is exact and refuses by name              (phases 92, 95, 102, 105-6)
    A SCHEMA LINE that names its units and leads with its
      distinction, because the model reads it literally       (phase 106's rule)
    AN EXEMPLAR that is RETRIEVED, not fixed, because a
      catalogue cannot grow inside a prompt                   (phases 101, 107, 108)

That is what "gradually swapping the set" turned out to mean. A library scales when
every machine carries its own way of being addressed — and the thing that finally made
twenty-six solvers behave like twenty-six solvers, rather than like the three the prompt
happened to illustrate, was a 384-dimensional nearest-neighbour lookup that costs no
model call at all.

================================================================================
PHASE 109 — the 1B can map, once the exemplar is fetched for it
================================================================================

The oldest open thread in this study is whether the small model can be made useful by
making the task mechanical. Phase 84 found OLMoE-1B answering at the parrot line on
one-bit reads; phase 98 saw it parse sixteen specs of thirty with three executing. Both
were before retrieval existed, and the 1B's failure was never arithmetic — the record
does that — it was knowing what to write.

A retrieved exemplar is exactly a template of what to write, in the shape of this
problem, for a machine that fits. So the phase 108 run was repeated with nothing changed
but the seat:

    OLMoE-1B alone                    1/27
    OLMoE-1B through MPEqs           12/27   (specs parsed 27/27, ran 17, wrong 5)
    Qwen-35B through MPEqs           25/27

    by class: datetime 3/3, probability 3/3, statistics 2/3, count 2/3, big 1/3,
    finance 1/3, convert 0/3, divisors 0/3, fractions 0/3

TWENTY-SEVEN OF TWENTY-SEVEN SPECS PARSED, against sixteen of thirty in phase 98. The 1B
went from producing JSON-shaped text to producing valid specs, and from one right answer
to twelve, on problems where it computes nothing at all. The classes it gets are the ones
whose exemplar is a near-template — a date subtraction, a dice probability, a variance —
and the ones it misses need the exemplar ADAPTED rather than copied, which is the
capability boundary drawn in the sharpest terms this study has managed.

So the answer to a question asked many phases ago — make it simple enough for a 1B to
apply — is a measured yes with a stated shape: the 1B can drive twenty-six exact
machines at forty-four percent on nine classes where it manages four percent by itself,
provided the arithmetic is not its job and the example is fetched, not remembered.

================================================================================
PHASE 110 — the band is about difficulty, not about class
================================================================================

Three more machines went in the way the recipe says — shape (standard figures with pi
kept SYMBOLIC, because the area of a circle of radius 5 is 25*pi and 78.54 is a
different answer), inclusion-exclusion (the survey problem, up to four sets), and phase
91's FORMULA LIBRARY, which had existed for fifteen phases and answered nothing until
today because nothing could address it. Each with a bank exemplar from the first minute.

Then the battery came back tied:

    easy instances (n = 15)     SOLO-35B 15/15      MPEqs 15/15

Which is the phase 105 rule from its negative side: a machine does not widen the band
unless the class is actually beyond the model. A circle of radius 7, three sets of a few
hundred, momentum of 12 kg at 7 m/s — the model does all of that unaided.

So the same three classes were regenerated with awkward fractions and large numbers,
changing NOTHING about the classes, the schemas or the bank:

    hard instances (n = 15)     SOLO-35B 7/15       MPEqs 14/15
    by class: shape 2 -> 5, inclusion 3 -> 5, formula 2 -> 4; MPEqs wrong 0

THE BAND IS DEFINED BY ARITHMETIC DIFFICULTY, NOT BY SUBJECT. The same machine, the same
mapping, the same prompt: at radius 7 it is redundant and at radius 47/6 it is the only
thing that works. Every earlier band result reads the same way in hindsight — GSM8K lost
because grade-school sums are easy, not because word problems are the wrong shape — and
the deployment rule that follows is not "route by topic" but "route by whether the
arithmetic will fit in the model's head".

================================================================================
PHASE 111 — the capability sheet: 174 against 74, assembled from the runs
================================================================================

Every band result lives in the JSON its own run wrote, so the summary is assembled from
the measurements rather than from memory — a stale claim shows up as a missing file
rather than as prose nobody rechecked.

    band                                   model  MPEqs    n
    Grade-school word problems                28     21   30
    Hard exact arithmetic (tuned)              0     15   20
    Hard exact arithmetic (held out)           0     17   20
    Probability and unit conversion            1     17   20
    Statistics, calendars, percentages         8     18   18
    Shape, sets, formulas (easy)              15     15   15
    Shape, sets, formulas (hard)               7     14   15
    Nine classes mixed, generic examples       7     20   27
    Nine classes mixed, retrieved              7     25   27
    The same 27, driven by a 1B                1     12   27
    AIME, model-mapped                         -      1   15
    AIME, hand-mapped ceiling                  -      5   15

    comparable totals                         74    174  219
    (with phase 113's fresh battery added)    77    197  243

Across every battery measured on both arms, the model answering alone gets 74 of 219 and
the same model mapping onto exact machines gets 174 — and the two rows where it loses are
the two where the arithmetic was never hard. The library is 29 solvers with 38 self-tests
exact and 11 named refusals, grown from 15 in eleven phases, every addition demanded by a
failure rather than chosen by taste.

The recipe, each part measured before being combined:

    a MACHINE that is exact and refuses by name
    a SCHEMA LINE that states its units and leads with its distinction, because the
      model reads it literally and has nothing else to go on
    an EXEMPLAR that is RETRIEVED, because a catalogue cannot grow inside a prompt
    a ROUTER that asks whether the arithmetic will fit in the model's head, since that
      and not the topic is what decides who should do the work

================================================================================
PHASE 112 — the router, and the simpler policy that beats it
================================================================================

Phase 110's law reads like a deployment instruction — route by whether the arithmetic
fits in the model's head — so the rule was written from the law BEFORE looking at any
outcome, reading nothing but the problem text (exactness words, a fraction, a number
past ten thousand, a wide range, calendar language), and scored over every problem both
arms had recorded. 135 of them, no model calls: the batteries stored their verdicts, and
the two batteries that stored no text draw their problems deterministically, so replaying
the draw recovers the strings exactly.

    policy                              right   rate   sent to the machinery
    always the model                       44   0.33   0
    always the machinery                  113   0.84   135
    coin flip (expected)                   78   0.58   ~67
    THE A PRIORI RULE                      95   0.70   80
    best single feature, leave-one-out     73   0.54   -
    MACHINERY, MODEL ON REFUSAL           117   0.87   124
    oracle (either arm right)             120   0.89   -

THE ROUTER LOSES TO NOT ROUTING. The rule I was confident in scores 0.70 where sending
everything to the machinery scores 0.84, because on the measured distribution the two
arms are not comparable at all: the machinery alone is right on 76 problems the model
misses and the model is right on 7 the machinery misses. Routing is only worth doing
between arms of similar strength, and these are not.

WHAT DOES BEAT IT IS THE THING THE MACHINES ALREADY DO. Take the machinery's answer
whenever the record produced one, and hand back to the model ONLY where the record
REFUSED — not where it answered wrongly, which would be conflating a shrug with a lie.
That reaches 117 of 135, three short of the oracle, and it needs no features, no
thresholds and no text analysis: the refusal IS the routing signal, and it was there
from phase 92.

So the deployment rule at the end of all this is one sentence, and it is not the one I
wrote at the start of the phase: map everything, execute what validates, and let the
model answer exactly what the record declined.

================================================================================
PHASE 113 — the policy, run live: 23 of 24, and not one wrong answer
================================================================================

Phase 112's rule was derived from a table, which makes it a hypothesis. This runs it as
a pipeline on twenty-four problems written after the rule was — eight classes, no reuse,
truths computed before any model call — with the whole recipe in place: retrieval picks
two exemplars, the model writes one spec, the record validates and executes, and the
model answers only what the record REFUSED.

    the model alone                    3/24
    the machinery alone               23/24
    the policy (model on refusal)     23/24

    record refused 1, the model rescued 0 of it; machine answers kept but wrong: 0

TWENTY-THREE OF TWENTY-FOUR, AND NOT ONE WRONG ANSWER. The single miss is a refusal —
the record declining rather than guessing — which is the failure mode this study has
been trying to buy since phase 89, and it cost one problem out of twenty-four to have.
The model answering the same questions alone gets three.

The fallback earned nothing here, and that is worth stating plainly rather than hiding:
with one refusal in twenty-four there was almost nothing to rescue, so the policy and
the bare machinery scored the same. The fallback is insurance, and this run did not
need it — the honest reading is that its value scales with the refusal rate, which the
band's own maturity has driven near zero.

What twelve phases of growth bought, end to end and on unseen problems: 3/24 becomes
23/24, with the wrong-answer count at zero and every number the record produced derived
by exact arithmetic from a spec a small model wrote.

================================================================================
PHASE 114 — four more classes: sequences, matrices, partitions, logarithms
================================================================================

The growth loop now has a rule and a recipe, so these four went in under both, chosen
where a model reliably fails and a machine reliably does not:

  SEQUENCE   fit the rule from the terms and extrapolate — and REFUSE when no rule
             explains EVERY term, because a rule that fits four of five is not the rule
  MATRIX     determinants, inverses, products and powers, exact in Fractions
  PARTITION  the counting DP a bounded search cannot reach: ways to make a total from
             given parts, ordered, unordered or distinct
  LOGEXP     digits of huge powers, trailing zeros of factorials, exact integer logs

    sixteen problems, truths computed before any model call

    SOLO-35B    3/16    sequence 0/4, partition 0/4, matrix 1/4, logexp 2/4
    MPEqs      15/16    sequence 4/4, partition 4/4, matrix 3/4, logexp 4/4

Partitions and sequences are shutouts: "in how many ways can 200 kroner be paid with
coins of 1, 5, 10, 20 and 50" has one answer, 2930, and no amount of fluency finds it.

TWO SELF-TEST FAILURES, BOTH MINE AGAIN. The quadratic sequence anchor read the index
one-based where the solver documents zero-based (462, not 420), and a three-by-three
determinant I wrote as -124 is -66 when worked by hand. The tally across the study is
nine hand-anchor errors against one machinery error, and every one of the nine was
caught by the machinery it was meant to check — which is the argument for writing the
truth first stated as a score rather than as a principle.

Library: 33 solvers, 45 self-tests exact, 13 refusals named.

================================================================================
PHASE 115 — four more: base arithmetic, approximation, word counting, primes
================================================================================

    BASEARITH  adding in base seven is a carry chain a model does by translating to
               decimal in its head and back, which is where it slips
    APPROX     the closest fraction under a denominator cap, and continued fractions
    STRCOUNT   distinct arrangements of a word with repeated letters
    PRIMES     counting primes below a bound, the nth prime, the next prime

    sixteen problems, truths computed before any model call

    SOLO-35B    5/16    approx 0/4, basearith 1/4, primes 1/4, strcount 3/4
    MPEqs      16/16    every class complete, nothing wrong

Sixteen of sixteen. The approximation class is the cleanest illustration of the band's
definition: "the closest fraction to 3126535/995207 with denominator at most 200" is
355/113, and there is no version of fluency that produces it — a Stern-Brocot walk does,
in a few dozen exact steps, and the model's only job was to notice which machine that is.

Library: 37 solvers, 51 self-tests exact, 14 refusals named.

================================================================================
PHASE 116 — the library splits: engine and domains
================================================================================

At 1,509 lines solvers2.py was approaching the point where a file stops being readable,
so it split along the seam it already had:

    solvers2.py  (886 lines)  THE ENGINE — the expression sandbox and its AST whitelist,
                              the pruned tuple search with its linear solve, the fold,
                              the named-step arithmetic, and the dispatcher that lets a
                              shape overrule a name
    solvers3.py  (640 lines)  THE DOMAINS — probability, unit conversion, statistics,
                              calendars, finance, shapes, set counting, the formula
                              library, sequences, matrices, partitions, logarithms,
                              base arithmetic, rational approximation, word counting,
                              primes

The split is mechanical and invisible from outside: solvers2 imports the domain registry
lazily inside its dispatcher, so run2 still reaches all 37 machines and not one of the
fifteen calling files changed except two that imported a unit parser by its old address.
51/51 self-tests exact and 14/14 refusals named after the move, and every domain
re-verified through the unchanged entry point.

================================================================================
PHASE 117 — the scaling law at thirty-seven machines
================================================================================

A scaling law that holds once is a coincidence until it holds again at a size that could
have broken it. Phase 108 measured the recipe at twenty-six solvers; the library is now
thirty-seven across seventeen classes, the catalogue is thirty-six lines long and the
bank holds twenty exemplars, which is exactly the crowding that should start to hurt.

    thirty-four problems, two from each class, all with truths computed before any
    model saw them; the whole catalogue in the prompt; two nearest exemplars retrieved

    the needed class was retrieved for  32/34
    SOLO-35B                            10/34
    MPEqs                               33/34   (parsed 34, ran 34, wrong 1)

    sixteen of seventeen classes complete; the single miss is one count problem

Ninety-seven percent at thirty-seven machines, against seventy-four percent of the same
problems that the model alone gets wrong. The prompt is four times the length it was at
phase 94 and the mapping is BETTER, because what the model has to hold has not grown:
it still reads one problem, sees two matched exemplars and writes one spec. Retrieval is
what keeps the working set constant while the library grows, which is the whole reason
the study's oldest instinct — keep as little as possible in working memory, store the
rest, and fetch what this problem needs — turns out to apply to the tool as well as to
the reasoning.

================================================================================
PHASE 118 — the small model at thirty-seven machines: it went UP
================================================================================

The library grew by eleven machines and the catalogue by a third since phase 109 put
OLMoE-1B in the driving seat, which is exactly the direction that should hurt a model
whose failure mode was never arithmetic but instruction-following. Same battery as the
35B run, same bank, same retrieval, same prefill — only the seat changes.

    OLMoE-1B alone                     2/34
    OLMoE-1B through MPEqs            18/34   (31 specs parsed, 22 ran, 4 wrong)
    Qwen-35B through MPEqs            33/34

    at twenty-six machines (phase 109) the same model scored 12/27 — 44 percent
    at thirty-seven machines it scores 53 percent

THE BIGGER LIBRARY HELPED THE SMALLER MODEL. Not because the 1B got better at anything,
but because more of the problems now have a machine whose exemplar is nearly a template:
sequences, matrices, primes, statistics, calendars, base arithmetic and approximation
all come in at 2 of 2. What it still misses are the classes where the exemplar has to be
ADAPTED — a fold with different constants, a conversion between units the example did not
mention — which is the same boundary phase 109 drew, unmoved by a third more catalogue.

So the recipe's cost to a small model is not the size of the library. It is whether the
retrieved example can be copied with the numbers changed, and that is a property of how
the exemplars are written, which is a thing anyone can improve without touching a model.

================================================================================
PHASE 119 — the small model's ceiling was the exemplar, and it moved
================================================================================

Phase 118 ended on a claim about the EXEMPLARS rather than the model: the 1B solves the
classes whose retrieved example is nearly a template and misses the ones where it must be
adapted. That is testable by rewriting five examples, so five were — big products, unit
conversion, factorial divisors, fraction folds, word arrangements — each phrased the way
those problems are usually phrased, each on different numbers and a different question
from anything in the battery. Nothing else changed: same model, same catalogue, same
machines, same thirty-four problems.

    OLMoE-1B alone                          2/34
    OLMoE-1B, phase 118 exemplars          18/34   (31 parsed, 22 ran)
    OLMoE-1B, five rewritten exemplars     23/34   (32 parsed, 28 ran)

    gains: big products, factorial divisors, word arrangements, partitions and
    logarithms each go from missed to solved; conversions and fraction folds do not

FIVE SENTENCES OF PROMPT BOUGHT FIVE PROBLEMS, at fifty-three percent to sixty-eight, on
a model that computes nothing. The ceiling was where the claim said it was, and the lever
is one no model release is needed to pull: write the example so it can be copied with the
numbers changed.

What still resists is instructive. Conversions stay at zero because the example's units
are never the problem's units — copying gets you "mile/second" where the question says
kilometres per hour — and fraction folds stay at zero because the fold's step expression
is the whole problem and cannot be lifted from an example at all. Those two are genuine
adaptation, and adaptation is where a 1B stops.

================================================================================
PHASE 120 — moving adaptation into the schema, and the bank's crowding cost
================================================================================

Phase 119's two resisting classes needed genuine ADAPTATION, which is the same defect
seen from the schema side — a slot that must be COMPOSED rather than FILLED. So both
slots changed, and the change is the study's oldest move applied to its own interface:

    convert now accepts the question's own words ("kilometres per hour"), normalised
      by the record, so the model copies instead of translating
    iterate now accepts "multiply" and "add" as named slots, so a fold is two numbers
      rather than an expression the model must compose

    OLMoE-1B, template exemplars, k=2          23/34   convert 0/2, approx 2/2
    OLMoE-1B, slot exemplars, k=2              23/34   convert 2/2, approx 0/2
    OLMoE-1B, slot exemplars, k=3              19/34

THE SLOT CHANGE DID EXACTLY WHAT IT PREDICTED AND THE BANK TOOK IT BACK. Conversions went
from zero of two to two of two — the model copied "kilometres per hour" straight out of
the question, which is all the new slot asks — and the approximation class fell from two
to zero, because two more bank entries pushed its exemplar out of the top two. Retrieval
hits fell 32 to 30 and the total stayed flat at 23. A bank has a crowding cost, and it is
paid by whichever class was nearest the margin.

THE OBVIOUS FIX MADE IT WORSE. Raising k to three restored the retrieval hits (30 back to
32) and dropped the score to 19: shown three examples the 1B copies the wrong one, taking
"iterate" for approximation problems because a fold was in view. A small model's exemplar
budget is small in both directions — too few and the class is unreachable, too many and
the nearest wrong one gets copied.

So the deployment note for small models is narrower than the recipe: retrieve exactly
two, and spend the bank's scarce room on slots that can be FILLED rather than on more
examples of composition. Folds stayed at zero throughout, and the honest reading is that
naming two slots was not enough for a step that varies in more than two ways.

================================================================================
PHASE 121 — Norwegian: the catalogue travels, the retrieval does not
================================================================================

Every battery so far has been in English, which is an assumption rather than a
measurement: the catalogue is English, the exemplars are English, and the mapping step is
the only place language enters at all. Eighteen problems across six classes were written
in Norwegian — faithful renderings of shapes the batteries already cover, new numbers,
truths computed here — and nothing else changed.

    the needed class was retrieved for   8/18   (against 32/34 in English)
    SOLO-35B                             6/18
    MPEqs                               15/18   (18 parsed, 16 ran, 1 wrong)
    by class: datetime 3/3, finance 3/3, probability 3/3, statistics 3/3,
              count 2/3, divisors 1/3

TWO LAYERS, TWO ANSWERS. The retrieval layer half-collapses — a MiniLM trained on English
puts "Hvor mange heltall fra 1 til 300000 er delelige med 19" nowhere near "How many
integers from 1 to 90000 are divisible by 19", so the right exemplar arrives less than
half the time. The mapping layer does not care: with the catalogue in view the model
reads the Norwegian problem and fills the English schema anyway, and the pipeline lands
83 percent against the model's 33.

That is a useful separation for anyone deploying this. The SCHEMAS carry across languages
because they describe structure, and structure is what the model is actually reading. The
BANK does not, because a nearest-neighbour lookup is only as multilingual as its encoder —
so a Norwegian deployment should write its exemplars in Norwegian, or embed them with a
multilingual encoder, and would then presumably recover the classes that fell here
(divisors at 1 of 3 is the visible cost).

================================================================================
PHASE 122 — the bank in Norwegian: the retrieval layer comes back
================================================================================

Phase 121 ended on a deployment note — write the bank in the user's language — and a note
that is not measured is an opinion. So the nine exemplars were translated, PROBLEM TEXT
ONLY: every spec stayed exactly as it was, because the schema is the interface and not
the language.

    the needed class was retrieved for   17/18   (English bank: 8/18)
    MPEqs                                16/18   (English bank: 15/18)
    SOLO-35B                              6/18   unchanged

    count 2/3 -> 3/3; every other class held; divisors stays at 1/3 for its own reasons

Translating nine sentences doubled the retrieval hit rate and bought one problem. The
small score gain is the honest headline: the mapping layer had already been compensating
for the broken retrieval by reading the catalogue directly, so fixing retrieval recovered
the margin rather than the middle. What it really buys is the SMALL model, which cannot
compensate — phase 119 measured the 1B living entirely off whether the retrieved example
can be copied, and an example it never sees cannot be copied at all.

The two layers, finally stated as a rule: SCHEMAS are structure and travel; EXEMPLARS are
surface and must be written in the language of the person asking. Both are cheap, and
neither needs a different model.

================================================================================
PHASE 123 — the same test on the small model: 0 to 4, where the big one moved 15 to 16
================================================================================

Phase 122's explanation was a claim about the SMALL model — the 35B had been compensating
for broken retrieval by reading the catalogue directly, and a 1B cannot — so it was tested
on the small model. Same eighteen Norwegian problems, English bank and then Norwegian.

                                 retrieval   solved
    OLMoE-1B, English bank          8/18      0/18
    OLMoE-1B, Norwegian bank       17/18      4/18
    Qwen-35B, English bank          8/18     15/18
    Qwen-35B, Norwegian bank       17/18     16/18
    either model answering alone       -      6/18 and 0/18

NINE TRANSLATED SENTENCES ARE THE DIFFERENCE BETWEEN NOTHING AND SOMETHING for the small
model, and the difference between fifteen and sixteen for the large one. The compensation
story is exactly right: a model that can read a thirty-six-line catalogue does not need
the example, and a model that copies needs nothing else.

The 1B's Norwegian score is still far below its English one (4 of 18 against 23 of 34),
so a translated bank recovers the retrieval layer without recovering the model — copying
an example whose sentence is in one language into a problem written in another is itself
an adaptation, and adaptation is the boundary phase 119 drew and phase 120 could not move.

Which closes the language question in the shape everything else in this study has taken:
the mechanical part travels, the part that needs judgement does not, and the whole art is
moving as much as possible from the second into the first.

================================================================================
WHAT MPEqs IS, AFTER PHASES 92-123 — the whole arc in one place
================================================================================

THE MEASUREMENT, assembled from the JSON each run wrote (see capability.py):

    comparable totals across every two-armed battery    118 model     316 MPEqs
                                                        of 369 problems

    where the machinery loses: grade-school word problems (28 against 21) and easy
    instances of any class (15 against 15) — both because the arithmetic was never hard
    where it wins: everything whose answer is an exact ratio, a large integer, a wide
    count, a calendar span or a structure the model cannot enumerate
    where neither reaches: competition mathematics, hand-mapped ceiling 5 of 15

THE LIBRARY: 37 solvers over 17 classes, 52 self-tests exact, 14 named refusals, split
across an engine (expression sandbox, pruned tuple search, folds, dispatch) and its
domains. It grew from 15 in thirty-one phases, and every single addition was demanded by
a failure someone measured rather than chosen because it seemed useful.

THE RECIPE, each part measured separately before being combined:

    A MACHINE that is exact and refuses by name. The refusal is not a consolation, it is
      the routing signal: taking the machine's answer and handing back only refusals beat
      every text-feature router tried (117 of 135 against 95).
    A SCHEMA LINE that states its units and leads with its distinction. Three separate
      phases lost problems to a buried k-versus-n, an unstated percent, an ambiguous
      rate. The model reads the schema literally and has nothing else.
    AN EXEMPLAR THAT IS RETRIEVED. A catalogue cannot grow inside a prompt: with generic
      examples two classes vanished entirely, with retrieval the same battery went 25 of
      27 with nothing wrong, and at 37 machines it went 33 of 34.
    A SLOT THAT CAN BE FILLED RATHER THAN COMPOSED, for small models. Naming "multiply"
      and "add" instead of asking for a step expression, and taking units in the
      question's own words, is what moves a 1B off zero.

THE SMALL MODEL, the thread that ran the longest: OLMoE-1B answers 1 of 27 alone and
drives 37 exact machines to 23 of 34 — and every gain came from making the example more
copyable, never from asking the model for more. Its ceiling is adaptation: it cannot
change an example's units, compose a fold's step, or cross a language boundary.

WHAT THE STUDY IS ACTUALLY ABOUT, visible now that the numbers are in: none of this is
about arithmetic. The record could always compute. Everything measured here was about
ADDRESSABILITY — whether a capability can be reached by the thing holding the problem.
The formula library sat unused for fifteen phases and answered nothing until it had a
schema line; the unit router the same; a machine nobody can name is not a capability,
and the whole engineering task turned out to be building addresses.

================================================================================
PHASE 124 — AIME once more, with everything: 2 of 5, and the diagnosis stands
================================================================================

Since phase 101 measured the mapper as the bottleneck on competition mathematics, the
library went from 20 machines to 37, the schemas learned to state their units, composed
slots became fillable ones, and exemplars became templates. None of that was aimed at
AIME, so the oldest open thread got one more measurement with all of it in place.

    retrieved the needed shape for the reachable five   5/5
    of those five, solved                               2   (phase 101: 1, phase 94: 0)
    overall: claimed 12 of 15, ran 8, exact 2, wrong 6, declined 2

Two of five. Everything the night produced — a library nearly doubled, schema lines
rewritten three times over, exemplars rebuilt as templates, a retrieval layer that never
misses — bought one additional AIME problem, and the two that come back are the two whose
mapping is nearest to transcription.

So phase 99's diagnosis stands as the honest last word: a third of these problems is
expressible, the machines are exact, the retrieval is perfect, and what is missing is the
step where a competition problem in language becomes a filled spec. That step is not a
prompt away, and thirty phases of improving everything around it have now measured how
much it is not.

The contrast with the same machinery's other results is the point rather than a
disappointment: 292 of 345 across the batteries, 23 of 24 on fresh hard-arithmetic
problems with nothing wrong, 33 of 34 across seventeen classes — and 2 of 15 here. The
tool has a domain, the domain has an edge, and the edge is exactly where the mapping stops
being transcription and starts being mathematics.

================================================================================
PHASE 125 — the equation solver becomes addressable, thirty phases late
================================================================================

Phases 80 to 84 built the both-sides machinery — equations as states, reversible moves,
residue-priced roads, ping-pong search — and measured a great deal about it. Then it sat
there. Nothing a mapped problem asked could reach it, for exactly the reason the formula
library and the unit router could not be reached before phases 105 and 110: no schema
line, no address.

So it has one now. A spec carries the equation AS THE PROBLEM WRITES IT — "3/2x + 7 =
5x - 2" — and the record parses both sides into exact coefficients, normalises, solves in
Fractions, verifies by substitution, and refuses an identity or a contradiction BY NAME
rather than returning a number for either.

    3/2x + 7 = 5x - 2        18/7
    -x/4 + 5 = 2             12
    2x/3 + 1/6 = x - 1/2     2
    2x + 3 = 2x + 5          refused: contradiction, no solution
    2x + 3 = 2x + 3          refused: identity

Library: 38 solvers, 54 self-tests exact, 16 named refusals.

Three capabilities have now been rescued from unaddressability this way — a formula
library, a unit router and an equation solver, each built carefully, each measured
thoroughly, each answering nothing at all until someone wrote the sentence that says how
to ask for it. That is the study's most repeatable finding and the one least likely to be
noticed while building: THE ADDRESS IS PART OF THE CAPABILITY.

================================================================================
PHASE 126 — the equation class, measured live: 12 of 12
================================================================================

A capability that has never been asked for by a model is still only half-built, so the
newly addressable equation solver was measured like every other class: twelve problems
with fractional coefficients — six written as equations and six as the sentences that
describe them — truths computed before any model call.

    SOLO-35B     4/12
    MPEqs       12/12   (12 parsed, 12 ran, nothing wrong)

Both halves came through: the model copies "7/3x - 5 = 1/2x + 4" straight out of the
symbolic problems, and for the worded ones it writes the equation the sentence describes
and hands that over — which is the division of labour this study has been circling since
phase 45, in its smallest possible form. The record does the algebra it has been able to
do since phase 80 and could not be asked for until yesterday.

Library: 38 solvers, 54 self-tests exact, 16 named refusals, across 18 classes.

================================================================================
PHASE 127 — three last classes: acceleration, roman numerals, check digits
================================================================================

    KINEMATICS  the five constant-acceleration relations, closed from any three knowns,
                exact — and refusing to report an irrational final speed as a decimal
    ROMAN       both directions, mechanical, and reliably fumbled past a few hundred
    CHECKSUM    Luhn, ISBN-10 and ISBN-13: weighted sums nobody carries in their head

    twelve problems, truths computed before any model call

    SOLO-35B     7/12   checksum 1/4, kinematics 3/4, roman 3/4
    MPEqs       12/12   nothing wrong

Check digits are the clean split, 1 against 4: "the ISBN-13 check digit for
978147674137" is a fixed alternating sum and a model that gets it right is guessing well
rather than computing.

Library: 41 solvers, 59 self-tests exact, 16 named refusals, across 21 classes — grown
from 15 machines in one night, every addition demanded by a measured failure.

================================================================================
PHASE 128 — the error bar: two runs of the same battery, row for row
================================================================================

Every number in this study comes from a single run, which is a methodological debt this
phase pays. The thirty-four-problem mixed battery was run a second time with nothing
changed — same seed, same prompts, same temperature zero, same model — and compared row
by row rather than only on the total, since two runs can agree on a score while disagreeing
on which problems they got.

    run 1        MPEqs 33/34, solo 10/34
    run 2        MPEqs 33/34, solo 10/34
    rows differing in outcome OR in which solver was chosen:   0

Not one row moved. The Metal nondeterminism noted early in this study (±3 on logit
comparisons) does not reach the level anything here is measured at: a spec is a short
structured object at temperature zero, and either the model writes the same one or it
does not. So the batteries can be read as exact counts rather than as samples, and the
comparisons between phases — 8 to 12 to 15, 18 to 23, 20 to 25 — are differences in the
pipeline rather than in the weather.

One caveat kept honest: this measures the SPEC-WRITING path, where output is short and
structured. The long free-form arms (the solo baselines, the AIME working) are not shown
to be as stable by this, only the mapping the whole apparatus depends on.

================================================================================
PHASE 129 — one command
================================================================================

Everything measured across thirty-eight phases, wired together in the order it was
measured in and given a command line:

    solve.py "How many days are there from 1 January 1970 to 10 August 2026?"   20675
    solve.py "Hvor mange positive divisorer har 22 fakultet?"                   96000
    solve.py "Solve 7/3x - 5 = 1/2x + 4 for x."                                 54/11

Retrieve two exemplars, show the catalogue, one spec from the model, exact execution by
the record, and the model's own answer only where the record refused. Exit code 2 on a
refusal nothing rescued, so it composes with other tools; --explain prints the spec, the
retrieved shapes and the machine's working.

A study is a pile of numbers until someone can run it. This is the smallest thing that
makes the numbers usable, and it fits on a line.

================================================================================
PHASE 130 — thirty AIME problems never touched: 0 for the mapper, 5 for the hand
================================================================================

Every olympiad number in this study came from one sample of thirty drawn with seed 5,
which the library and the exemplars have been shaped around at one remove. The other
thirty problems in the cached set had never been read by anything here, and they got the
finished machine exactly as it stands — 41 solvers, the full catalogue, the retrieval
bank, one spec each, executed exactly.

    SOLO-35B, visible working    3/30
    MPEqs                        0/30   claimed 23, ran 13, ALL 13 wrong,
                                        refused 10, declined 5, unparseable 2
    either arm right             3/30

Zero. The prediction written before the run was two to five, and the sample said none.

THEN THE SAME FIVE PROBLEMS WERE MAPPED BY HAND, and all five came back exact:

    [11]  triangles among 20 circle points whose labels differ by a prime      72
    [15]  subsets of 1..10 with exactly one consecutive pair                  235
    [18]  40 lines, points where exactly two of them cross                    607
    [19]  n < 1000 whose remainders mod 2,3,4,5,6 are all distinct             49
    [21]  six trees in arithmetic sequence, greatest double the least         220

Five of thirty are expressible in the vocabulary as it stands — the same one-sixth phase
99 measured on the other sample, replicated on problems nothing here has seen. And the
mapper produced a spec for all five and got all five wrong: 24 for 72 (it dropped one of
the three prime conditions), 35 for 235 (it counted something else entirely), a decline
for a problem that is one subtraction, 0 for the remainders (it reached for the
single-variable search and could not express five simultaneous inequalities), and the
series solver for an arithmetic-sequence word problem it then failed to fill.

So the study's headline result about competition mathematics survives contact with an
unseen sample, in the sharpest form yet: THE VOCABULARY REACHES ONE PROBLEM IN SIX AND
THE MAPPER REACHES NONE OF THEM. Everything between language and a filled spec is where
the remaining work is, and thirty-eight phases of improving the machines, the schemas,
the exemplars and the retrieval have moved that number from zero to zero.

Worth stating plainly beside it: on the classes where the mapping IS transcription, the
same pipeline is at 316 of 369. The difference between those two numbers is the whole
finding.

================================================================================
PHASE 131 — mapping by role, not by word: the verb, the subject, the relations
================================================================================

The bank has been keyed on masked surface TEXT since phase 88, and two measurements had
already named that as the wrong key without my noticing: Norwegian retrieval collapsed to
8 of 18 where English scores 32 of 34, and olympiad problems retrieved nothing useful
because their wording resembles nothing in the bank while their STRUCTURE resembles it
exactly. Both are lexical failures of a lexical key.

So the key became the structure: ACTION (count, sum, minimise, solve, convert,
probability), OBJECT (integer, pair, subset, divisor, date, word, matrix), RELATIONS
(divisibility, remainder, primality, distinctness, ordering, digits, equation, unit) and
SCOPE — the verb, the subject and the relations between them, extracted in one small
model call and matched by role agreement with no embedding anywhere.

THE FIRST THING IT DID WAS PROVE THE POINT ON A PAIR OF SENTENCES:

    "Hvor mange heltall fra 1 til 300000 er delelige med 19 og gir rest 4..."
    "How many integers from 1 to 400000 are divisible by 13 and leave remainder 4..."
    both -> {count, integer, [divisibility, remainder], range}    IDENTICAL

Retrieval hit rates, same problems, same bank, only the key changed:

    set              scored   text key   role key   both together
    Norwegian           18        6         15           16
    Mixed English       34       32         26           32
    Fresh AIME           5        3          4            3
    TOTAL               57       41         45           51

THE KEYS FAIL IN OPPOSITE DIRECTIONS, WHICH IS WHY THE SUM BEATS EITHER. A text key is
excellent when the surface is familiar and the language matches — 32 of 34 on English —
and worthless the moment either fails: 6 of 18 across a language boundary. A role key
does not care what language the sentence is in (15 of 18) or whether the story is about
apples, and it blurs classes the words would have separated (statistics and finance are
both "find_value" on an "amount", so it drops to 26 on English). Together: 51 of 57,
better than either alone on every set but the smallest.

The correction was right and the measurement is sharper than the correction: mapping
should be against roles because roles are what survives translation and rewording — and
the words should be kept anyway, because when they ARE reliable they separate structures
that share a verb.

================================================================================
PHASE 132 — grounded roles: the abstraction indexes instead of paraphrasing
================================================================================

Phase 131's signature threw the sentence away, which breaks the oldest rule here: every
step reversible, the residue kept. A role that cannot say WHICH WORDS produced it cannot
be checked, cannot be mapped back, and cannot hand the record anything to fill a slot
with.

So the problem is tokenised with punctuation as its own indexed tokens, and every part of
the signature carries the span it was read from:

    "Hvor mange heltall fra 1 til 300000 er delelige med 19 og gir rest 4 når de
     deles på 8?"        21 tokens

    action    count        [0,2]   "Hvor mange heltall"
    object    integer      [3,6]   "fra 1 til 300000"
    relation  divisibility [7,10]  "er delelige med 19"
    relation  remainder    [11,14] "og gir rest 4"
    quantities  lower_bound=1, upper_bound=300000, divisor=19, remainder=4
                — pulled from the text BY INDEX, not re-read

Eighteen problems, nine Norwegian and nine English:

    signatures parsed                        18/18
    spans that index real tokens             72/72
    quantity claims landing on a number      49/49
    numbers recovered of those present       48/58
    coverage of the sentence by roles        0.71 to 1.00

AND THE VALUE ECHO FIRED IMMEDIATELY, on the very first problem: the model claimed a
number at index 18, which is the word "på"; the number 8 is at 19. The record dropped the
claim because a quantity must land on a token that IS a number, and nothing about that
check needs a second opinion. Off-by-one is the characteristic error of index-writing,
and it is now the characteristic error of something mechanical rather than of an answer.

What this buys, beyond checkability: the record can fill a spec's slots from the spans
itself — the bounds, the divisor, the remainder are all sitting at known indices with the
roles the model attached to them — which is phase 120's lever (move the adaptation into
the record) applied to parameters rather than to schemas. The model says WHERE, the
record reads WHAT, and the sentence is never lost because the abstraction points at it
instead of replacing it.

================================================================================
PHASE 133 — a node per word: does the graph carry anything the labels did not?
================================================================================

Phase 132 grounds each role in a SPAN, which is honest labelling of chunks and not a
graph: "[0,2] -> action" has no nodes, no edges, and nothing that says what points at
what. So the same problems were given a node per token and an edge per attachment —
every content token names its head and the label of that attachment, from a closed
vocabulary — and the record then tried to DERIVE the quantity roles from graph position
alone, with the model never naming them.

    graphs parsed                          6/6
    malformed edges                        0
    graphs with exactly one root           6/6
    CYCLES                                 11, all in one graph
    numbers placed by graph position       16/21
    roles read off the shape, vs truth     13/21

    the same six problems by span labelling, same truth:   14/21

SO THE GRAPH IS REAL BUT IT IS NOT YET BETTER. Thirteen roles against fourteen is a tie
inside the noise of six problems, and the honest reading is that a per-word graph
recovers about as much as naming the chunks does — while costing a longer prompt, a
larger output and a structural failure mode the span version cannot have. Eleven cycles
in one graph is a thing that simply cannot happen when the model only labels spans.

What the graph DOES buy, and the span version cannot: the failure is VISIBLE. A cycle is
a mechanical fact about the structure, checkable with no truth to compare against, and it
localises to the tokens involved. Span labelling has no equivalent — a mislabelled chunk
looks exactly like a correct one until an answer comes out wrong. That is the same trade
this study measured at phase 96 between social and mechanical gates, one level further
down.

The question was whether we build a graph per word or map long sentences to an action.
The answer was that we mapped chunks, and now both exist and have been compared on the
same problems: chunks are cheaper and currently tie on accuracy; the graph is auditable.
Neither is finished, and the useful direction is the one the numbers point at — keep the
spans for retrieval and slot-filling, keep the graph for checking, and stop pretending
the first was ever a graph.

================================================================================
PHASE 134 — the graph audits the spans: precise, not eager
================================================================================

Phase 133 left both readings on the table with a tie between them and one asymmetry: the
graph can expose itself, spans cannot. So the graph became the auditor, with four
mechanical cross-checks that need no answer key at run time — a relation span must contain
the edge it implies (divisibility against divisor, remainder against modulus), a number's
span-role must match its graph position, the action span must reach the root or a target,
and the structure must have one root, no cycles and no edges off the token list.

    problems audited                     6, five carried a flag (9 flags in total)
    span quantity roles scored           20 (6 genuinely wrong, 14 right)
    CATCH   wrong roles flagged          2/6
    COST    right roles flagged          0/14

ZERO FALSE FLAGS. The auditor never once objected to a reading that was correct, which is
the property phase 96 said to measure first and the one that decides whether a second
opinion is a gate or a nuisance. What it costs is sensitivity: four of six real errors
went through, because the rules only fire where the two vocabularies overlap — a relation
that implies no particular edge makes no claim, and silence is not agreement.

That trade is the right way round for this pipeline. A flag here means STOP with a named
token and a named rule, and it has so far never meant stop wrongly; a missing flag means
only that this particular check had nothing to say. The three flag kinds that did fire —
relation against edge, quantity against position, and pure structure — are each localised
to a token, so a flagged problem hands back where to look rather than a shrug.

================================================================================
PHASE 135 — islands and roads: a machine may now be an edge
================================================================================

For forty phases the pipeline mapped every problem onto exactly ONE machine, and phase 92's
hand-built search-into-factor chain sat there as proof that chaining works and nothing
could find it. Two halves close that, and both are mostly other people's work:

  EXECUTION. relgraph's topological resolver already ran a system of named definitions
  exactly, refusing cycles, undefined references and translations that drop a number, all
  with perfect ground truth. What it could not do is let a MACHINE be an edge. A definition
  may now be `@solver{slot=value, ...} report`, resolved in the same order and executed by
  run2, so phase 92's chain is three lines the record runs by itself:

      n   = 13
      s   = @search{domain={divisors_of_factorial, k=n}, quotient_is_square} sum
      ans = @factor{n=s} exponent_sum                                     -> 12

  DISCOVERY. When nobody supplies the chain, a bidirectional frontier over island TYPES
  finds one — forward from what the problem holds, backward from what it asks. The road
  matrix is generated a cell at a time as a rim asks for it and never stored: 246 cells
  computed across five routes against the 1,681 that were never built. That is the MPEE
  discipline as a number rather than an intention.

    4/4 graphs exact, 5/5 refusals named, 5/5 routes found
    41 machines typed as roads (consumes -> produces), no solver rewritten

FOUR BUGS, ALL MINE, ALL CAUGHT BY THE SELF-TEST BEFORE ANY MODEL SAW IT:

  Arithmetic edges divided in floats, so 20675 / 7 came back as the binary expansion of a
  rounded double — fixed by routing the expression through the phase 102 sandbox that
  already rationalises literals.
  An undefined reference fell through to "not arithmetic" instead of naming the missing
  name; the message that replaced it is strictly better and the test moved to match it.
  Type widening ran in both directions, so wanting an integer counted as wanting a count,
  the two rims met on their starting types and the router reported a route with an empty
  road. Widening is forward only: a count IS an integer, an integer is not a count.
  And substitution ignored word boundaries — a reference named "n" rewrote the middle of
  "exponent_sum", and the AIME chain came out at 480.

That last one is the phase's real lesson repeated at the level of the plumbing: an index,
a boundary and a name are the three things this study keeps getting wrong, and the only
reason none of them shipped is that the truth was written before the code ran.

================================================================================
PHASE 136 — chaining measured, split in two on purpose
================================================================================

Sixteen problems that genuinely need two machines, truths computed from the structure
before any wording was written, and split deliberately because "chaining works" and
"chaining works where the mapping works" are different claims this study has confused
before.

    half         n    solo   one-shot   GRAPH   note
    practical    8      1        0        5     two machines, inside the measured band
    olympiad     8      1        0        1     two machines, competition wording
    TOTAL       16      2        0        6     graph arm executed 16 steps, 2 wrong

ONE-SHOT SCORES ZERO, WHICH IS THE POINT OF HAVING IT. Today's pipeline maps to exactly one
machine and these problems need two, so its column is the floor a chain has to beat, and
it is a floor at zero rather than an argument.

THE GRAPH ARM BEATS IT — AND ONLY ON ONE HALF. Five of eight practical against one of
eight olympiad, from the same prompt, the same executor and the same catalogue. The split
you asked for is what makes that readable: chaining is not the thing competition problems
were missing. Phases 94, 130 and 124 all said the mapping is the bottleneck there, and
giving the mapper a second machine to reach for does not help a mapper that cannot express
the first one.

Where it does work it works cleanly: a date span into a percentage, a divisor sum into a
digit root, a prime count into a fraction of the range, an arrangement count divided
down — each two definitions the record ran in topological order, each exact, none of them
expressible as a single spec at all.

Two wrong deliveries out of eight that produced a number, both in the half where the
mapping is honest, and both from a first step that was right feeding a second step that
misread what it was given. That is the cost of chaining stated plainly: a chain multiplies
the ways a mapping can be wrong, and it does not multiply the ways it can be checked.

================================================================================
PHASE 137 — the external control: chaining loses on wording I did not choose
================================================================================

Phase 136's practical half was a battery I wrote, which is the self-confirmation the plan
flagged in advance, so the graph arm went to GSM8K: multi-step by construction, written by
strangers, and already measured twice on the same thirty problems with the same seed.

    the model alone (phase 98)      28/30
    one-spec pipeline (phase 98)    21/30
    GRAPH arm                       15/30   (26 systems parsed, 17 ran, 2 wrong,
                                             17 of those used more than one step)

CHAINING LOSES HERE, AND THE REASON IS NOT THAT CHAINING IS BAD. Grade-school problems
mostly need ONE spec with a few named steps inside it, which the arith machine already
does; asking for a system of definitions buys nothing and costs a heavier output format
that failed to parse or resolve thirteen times out of thirty.

Put beside phase 136 the law is clean and narrow:

    a chain wins only where a single spec is STRUCTURALLY impossible
    where one spec suffices, the heavier format loses to it

My own two-step battery satisfied the first clause by construction — one-shot scored 0 of
16 there because those problems cannot be written as one spec — and GSM8K satisfies the
second, where one-shot scores 21 and the graph 15. Both numbers are real and neither
generalises past its clause.

That is exactly what the external control was for. Without it the honest-looking sentence
"the graph executor beats the one-spec pipeline five to nothing" would have shipped, and
it would have been true only of problems I wrote to be true of.

================================================================================
PHASE 138 — a closed vocabulary: the role call becomes a lookup
================================================================================

The observation is not this study's: with a small closed vocabulary the map from word to
embedding is a TABLE, so you can go out to an embedding and read the word straight back,
and the network before and after stops being needed. A transformer already works this way
— its input matrix is a dictionary-sized word-to-vector table and its output projection is
that table read backwards — so a closed vocabulary changes the SIZE and the
INVERTIBILITY, not the mechanism.

The consequence here is exact: phase 131 spends one model call per problem turning a
sentence into a role signature. If the vocabulary is closed, that call is a lookup. So the
table was written from what the words MEAN, in both languages, never from which problems
failed — 134 entries, 17 of them carrying more than one role, because a word with several
senses gets several entries and that ambiguity is the point.

    set          table    model    words found    action+object agreement
    Norwegian     9/18    15/18       18/18            22/36
    English      26/34    26/34       34/34            44/68
    TOTAL        35/52    41/52

    model calls: 52 for the model key, ZERO for the table

ON ENGLISH THE TABLE TIES THE MODEL EXACTLY, 26 to 26, for free. A hundred and thirty-four
words replace a call per problem and lose nothing measurable — which is the strongest
version of this study's oldest move, since here the record does not merely check the
model's work but does it instead.

On Norwegian it loses, 9 to 15, and the reason is coverage rather than principle: the
table holds about ninety English words and forty Norwegian ones. The fix is more entries,
not a better model, and it is the same shape as phase 122 — translate the surface and the
lexical layer comes back.

What that says about the bigger idea: a full dictionary-sized table is not a different
kind of thing from what the model already carries, it is the same table made readable. The
saving is not in intelligence, it is in not paying a forward pass to look up something
that was always a lookup.

================================================================================
PHASE 139 — does the WORD predict its experts? The other prefetch axis, measured
================================================================================

If the word-to-vector step is a table rather than a forward pass, every token of a prompt
is in hand at time zero, in parallel, before a single layer runs — and the obvious systems
prize is the one this repository's other half exists for: start fetching experts from disk
earlier. The README's headline table tested prefetch ONE way, layer L predicting layer
L+1 (0.66 of 5.96 bits, dead). It never asked whether the TOKEN predicts the experts,
which is the only predictor available at lookup time.

Measured on the corrected contribution trace — 16 layers, 64 experts, top-8, 8,192
positions aligned against the same corpus through the same tokeniser, table built on the
first half and tested on the second:

    prefetch budget 16 of 64 experts (25% residency)
    token-conditioned    49.5%
    frequency only       52.7%
    difference           -3.1 points

KNOWING THE WORD MAKES IT WORSE. A per-token table is a noisy estimate of the global
frequency it is trying to beat, and 25,184 of 65,536 test records held a token the first
half never saw at all.

The diagnostic behind the number is the part worth keeping. For tokens repeating at least
five times, how much do their expert sets overlap with THEMSELVES, against two random
selections at the same layer?

    layer  0    same token 0.18     random pair 0.16
    layer  1    same token 0.19     random pair 0.17
    layer  8    same token 0.26     random pair 0.22
    layer 15    same token 0.31     random pair 0.26

THE SAME WORD, AT THE SAME LAYER, ROUTES ALMOST WHEREVER IT LIKES. Two points of overlap
above chance is all the word's identity is worth, and it stays that small at layer 0 —
because the MoE sits after attention, so even the first block's router is reading a
residual that already carries the sentence. There is no layer at which a word has its own
activation.

Which settles the extension too: an index of higher-layer activations PER WORD cannot be
built, not because it is expensive but because the thing being indexed does not exist. The
input embedding is a table and is precomputable and invertible — that part is real, and
phase 138 spent it — but one block up the activation is a function of the word AND its
neighbours, and the measurement above is how far apart the same word's two occurrences
already are.

Both prefetch axes are now measured and both are dead: the layer before tells you 0.66 of
5.96 bits, and the word itself tells you three points less than counting. What survives is
what already survived — static frequency pinning, which needs no prediction at all.
